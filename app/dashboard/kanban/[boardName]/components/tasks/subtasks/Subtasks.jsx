import Subtask from "./Subtask";

export default function Subtasks({ taskId, boardId, uid }) {
  return (
    <>
      {/* {subtasks.map((subtask) => {
        return (
          <Subtask
            key={subtask.id}
            task={subtask}
            taskId={taskId}
            boardId={boardId}
            uid={uid}
          />
        );
      })} */}
    </>
  );
}
